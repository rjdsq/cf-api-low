# CF聚源API

Cloudflare 节点聚合与分发面板，将多个api接口-优选ip -域名混合整合成一个优选优选节点的动态地址，实时更新，自动查询节点 IP 归属地并转为中文与国旗或者选择显示模板。

## 部署步骤（推荐 Pages 文件夹上传）

1. **上传代码**：本地建个空文件夹，把 `_worker.js` 放进去。在 CF 控制台进入 **Workers 和 Pages** -> **创建应用程序** -> **Pages** -> **上传资产**，上传文件夹并部署。
2. **建 KV 空间**：在 CF 控制台创建一个 KV 命名空间。
3. **绑定 KV（最关键）**：进入刚部署的项目 **设置 -> 函数 -> KV 命名空间绑定**。**变量名称必须严格填写为 `PK`**，并选择你刚建的 KV 空间。

*(备选方案：你也可以直接用fork本项目， Git 连 Pages，或者创建 Worker 直接贴代码，但第 2 和第 3 步绑定 `PK` 绝对不能漏。)*

## 后台使用

- **默认后台地址**：`你的域名`
- **默认后台地址**：`你的域名/admin`
- **默认密码**：`123`
- 

## APP使用

- **下载终端app后输入域名或者后台地址可直接进行管理和查看**